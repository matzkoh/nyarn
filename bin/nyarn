#!/usr/bin/env bash

if [ $1 = "npm" -a -f yarn.lock ]; then
  echo "The project has lockfile created by yarn." >&2
  echo "You should use yarn instead of npm." >&2
  exit 1
elif [ $1 = "yarn" -a -f package-lock.json ]; then
  echo "The project has lockfile created by npm." >&2
  echo "You should use npm instead of yarn." >&2
  exit 1
fi

"$@"
